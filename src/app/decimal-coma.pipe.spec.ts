import { DecimalComaPipe } from './decimal-coma.pipe';

describe('DecimalComaPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalComaPipe();
    expect(pipe).toBeTruthy();
  });
});
