import { StringifyOptions } from 'querystring';
import { NodeCompatibleEventEmitter } from 'rxjs/internal/observable/fromEvent';

export class AcabadosModel {
    
    id: number;
    acabadocategorias_id: number;
    rutaimagen: string;
    nombre: string;
    enlaces_id: number;
    rutabase: string;
    active: boolean;
    
}