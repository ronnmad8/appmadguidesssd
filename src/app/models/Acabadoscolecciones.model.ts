import { StringifyOptions } from 'querystring';
import { NodeCompatibleEventEmitter } from 'rxjs/internal/observable/fromEvent';

export class AcabadoscoleccionesModel {
    
    id: number;
    articulocolecciones_id: number;
    acabados_id: number;
    orden: number ;
    nombre: string;

}